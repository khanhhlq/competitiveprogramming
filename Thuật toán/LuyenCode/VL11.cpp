#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, dem = 0;
    cin >> n;
    if (n <= 1)
    {
        while (i <= trunc(sqrt(n)))
        {
            if (n % i == 0)
                dem++;
            i++
        }
    }
    else
    {
        cout << "NO";
    }
}